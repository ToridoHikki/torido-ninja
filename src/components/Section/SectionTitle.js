import React from "react";

const SectionTitle = ({title}) => {
    return (

        <div style={{fontSize: 25, color:"#03a9f4"}}>
            {title}
        </div>
    );
};

export default SectionTitle;