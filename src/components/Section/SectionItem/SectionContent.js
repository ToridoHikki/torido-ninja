import React from "react";

const SectionContent = ({content}) => {
    return (
        <div style={{textAlign: "left", margin:"0 20px 0 20px",fontSize: 15}}>
            {content}
        </div>
    );
};

export default SectionContent;