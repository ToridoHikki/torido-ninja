import React from "react";

const SectionProject = ({name}) => {
    return (
        <div style={{textAlign: "left", margin:"0 20px 0 20px",fontSize: 24,fontWeight:"bold"}}>
            {name}
        </div>
    );
};

export default SectionProject;